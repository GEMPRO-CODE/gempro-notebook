\ifextended
\templateentry
{Dijkstra}
{\texttt{dijkstra(g, s)} retorna \texttt{dist} para \texttt{g[v] = \{to, w\}} em $O((V+E)\log V)$.}
{Menores caminhos com pesos não negativos. Passe a lista de adjacência \texttt{g}, onde cada aresta é \texttt{\{to, w\}}, e a origem \texttt{s}; o retorno é o vetor \texttt{dist}. Vértices inalcançáveis ficam com um valor grande. O snippet retorna só as distâncias; se precisar reconstruir caminho, mantenha o pai fora da rotina. Complexidade: $O((V + E)\log V)$.}
{graph/dijkstra.cpp}

\templateentry
{0-1 BFS}
{\texttt{bfs01(g, s)} para pesos \texttt{0/1} em \texttt{g[v] = \{to, w\}}; $O(V+E)$.}
{Menores caminhos quando cada aresta tem peso \texttt{0} ou \texttt{1}. Interface: passe a adjacência \texttt{g} nesse formato e a origem \texttt{s}; o retorno é \texttt{dist}, com \texttt{dist[v]} igual ao menor custo até \texttt{v}. Vértices inalcançáveis ficam com um valor grande. Complexidade: $O(V + E)$.}
{graph/bfs01.cpp}
\fi

\templateentry
{Grafo com sequência de graus}
{Constrói um grafo a partir de uma sequência de graus. Complexidade: $O(N + M)$.}
{Constrói um grafo a partir de um vetor $d$ de tal forma que $\deg(v_i) = d_i$ para cada $i$. Complexidade: $O(N + M)$.}
{graph/havel-hakimi.cpp}

\templateentry
{Caminho Euleriano}
{\texttt{adj[v] = \{to, id\}}. Retorna a ordem de um caminho euleriano em $O(V+E)$.}
{Um Caminho Euleriano é um caminho que passa por todas as arestas de um grafo uma única vez cada. Passe uma lista de adjacência no formato \texttt{\{vizinho, idDaAresta\}}; em grafos não direcionados, use o mesmo id nas duas direções da aresta. \texttt{eulerWalk(g, m, s)}, onde \texttt{g} é a lista de adjacência e \texttt{m} é o número de arestas, retorna um caminho Euleriano que começa em \texttt{s}, se existir. O retorno é a ordem dos vértices/arestas do caminho encontrado. Complexidade: $O(V + E)$.}
{graph/eulerian-walk.cpp}

\extendedonly{
\subsubsection{Encontrando qualquer caminho euleriano}
Em grafos direcionados:
\begin{itemize}
	\item Se exatamente um vértice tem $d_{out} - d_{in} = 1$, então comece nesse vértice.
	\item Se todos os vértices tem $d_{out} = d_{in}$, pode começar em qualquer vértice com $d_{out} > 0$.
	\item Caso contrário, não existe caminho euleriano.
\end{itemize}
Em grafos não direcionados:
\begin{itemize}
	\item Se exatamente dois vértices tem $deg$ ímpar, então comece em qualquer um deles.
	\item Se todos os vértices tem $deg$ par, pode começar em qualquer vértice com $deg > 0$.
	\item Caso contrário, não existe caminho euleriano.
\end{itemize}
}

\templateentry
{Árvore de pontes}
{\texttt{comp[v] = }componente de \texttt{v} na árvore de pontes. Ponte \texttt{uv} sse \texttt{comp[u] != comp[v]}. $O(V+E)$.}
{Uma ponte é uma aresta que, quando removida, deixa o grafo desconexo. Se removermos todas as pontes de um grafo, o resultado são algumas componentes conexas. Podemos comprimir cada uma dessas componentes conexas em um vértice representante, e adicionar as pontes de volta. O resultado é a árvore de pontes. Dadaa a lista de adjacência \texttt{g}, \texttt{bridges(g)} retorna \texttt{comp}, onde \texttt{comp[v]} é a componente de \texttt{v} na árvore de pontes. Uma aresta \texttt{(u, v)} é ponte sse \texttt{comp[u] != comp[v]}. Complexidade: $O(V + E)$.}
{graph/bridges.cpp}

\templateentry
{Árvore de bloco e corte}
{\texttt{t[i] = }a quais componentes biconexas o vértice \texttt{i} pertence. \texttt{t[i].size() > 1} quando \texttt{i} é um ponto de articulação. Quando \texttt{i >= n}, \texttt{t[i]} são os vértices na componente biconexa \texttt{i}. $O(V+E)$.}
{Uma componente biconexa em um grafo é uma componente que permanece conexa mesmo depois de remover um vértice qualquer dela. Um ponto de articulação é um ponto que, quando removido, deixa o grafo desconexo. \texttt{bct(g)} retorna a árvore de bloco e corte \texttt{t}. Os nós \texttt{0..n-1} continuam sendo os vértices originais, e cada nó novo \texttt{i >= n} representa uma componente biconexa. Assim, \texttt{t[v]} lista as componentes biconexas que contêm o vértice original \texttt{v}, e \texttt{t[id]} lista os vértices originais pertencentes ao bloco \texttt{id}. Para ir de uma componente biconexa a outra, é necessário passar por um ponto de articulação. Em particular, \texttt{t[v].size() > 1} sse \texttt{v} é ponto de articulação. Componentes isoladas de um único vértice também geram um nó de bloco. Complexidade: $O(V + E)$.}
{graph/block-cut-tree.cpp}

\templateentry
{Dominator Tree}
{\texttt{domTree(g, s)} retorna o dominador imediato; \texttt{-1} para inalcançáveis. Quase linear.}
{Em um grafo direcionado com raiz, um vértice $u$ é um dominador do vértice $v$ se todo caminho da raiz até $v$ passa por $u$ obrigatoriamente. O dominador mais próximo de um vértice é chamado dominador imediato. \texttt{domTree(g, s)} retorna a dominator tree do grafo a partir do vértice \texttt{s} no formato \texttt{idom}, onde \texttt{idom[v]} é o dominador imediato de \texttt{v} no subgrafo alcançável a partir de \texttt{s}. O próprio \texttt{s} sai com \texttt{idom[s] = s}, e vértices inalcançáveis ficam com \texttt{-1}. Complexidade: quase linear, $O((V + E)\alpha(V))$ na prática.}
{graph/dominator-tree.cpp}

\ifextended
\templateentry
{Árvore geradora mínima direcionada}
{\texttt{dmst(n, r, e)} retorna o custo da arborescência mínima, ou \texttt{-1}; $O(VE)$.}
{Chu--Liu/Edmonds para MST direcionada. Interface: monte o vetor \texttt{e} de arestas no formato \texttt{E\{a, b, w\}}, onde \texttt{a -> b} tem custo \texttt{w}, e chame \texttt{dmst(n, r, e)}. O retorno é o custo mínimo de uma arborescência que alcança todos os vértices a partir da raiz \texttt{r}; se algum vértice não puder ser alcançado a partir de \texttt{r}, a função retorna \texttt{-1}. O snippet retorna só o custo, não as arestas escolhidas. Complexidade: $O(VE)$.}
{graph/directed-mst.cpp}
\fi

\templateentry
{Árvore geradora mínima direcionada rápida.}
{\texttt{dmst(n, r, es)} retorna o custo da arborescência mínima, ou \texttt{-1}; quase linear na prática.}
{Versão mais rápida de Chu--Liu/Edmonds com heaps meldáveis. Interface: monte o vetor \texttt{es} com arestas no formato \texttt{DE\{a, b, w\}}, onde \texttt{a -> b} tem custo \texttt{w}, e chame \texttt{dmst(n, r, es)}. O retorno é o custo mínimo de uma arborescência enraizada em \texttt{r} que alcança todos os vértices; se algum vértice não puder ser alcançado a partir de \texttt{r}, a função retorna \texttt{-1}. O snippet retorna só o custo, não a lista de arestas escolhidas. Complexidade: $O(E\log(V))$.}
{graph/fast-dmst.cpp}

\templateentry
{Floresta de grafo funcional}
{\texttt{par[i] = }imagem de \texttt{i}. \texttt{root[i] = }representante do ciclo/componente de \texttt{i}. $O(n)$.}
{Interface: construa \texttt{FuncForest ff(f)}, onde \texttt{f[i]} é o único vizinho de saída de \texttt{i} no grafo funcional. O snippet copia esse vetor para \texttt{ff.par}. Depois da construção, \texttt{ff.root[i]} guarda um representante do ciclo alcançado a partir de \texttt{i}; dois vértices estão na mesma componente funcional sse têm o mesmo \texttt{root}. Útil para agrupar vértices por ciclo destino antes de montar lifting, DP reversa ou contagens por componente. Complexidade: $O(n)$.}
{graph/functional-forest.cpp}

\templateentry
{Componentes fortemente conexas e grafo de condensação}
{\texttt{cnt = }número de SCCs. \texttt{comp[v] = }SCC de \texttt{v}. \texttt{condGraph = }DAG. $O(V+E)$.}
{Uma componente fortemente conexa em um grafo direcionado é uma componente em que é possível chegar a qualquer vértice começando em qualquer vértice. \texttt{scc(g)} retorna \texttt{\{cnt, comp\}}, onde \texttt{cnt} é o número de componentes e \texttt{comp[v]} é a componente de \texttt{v}. Depois, use \texttt{condGraph(g, cnt, comp)} para construir o grafo de condensação sem arestas repetidas. Complexidade: $O(V + E)$.}
{graph/scc.cpp}

\templateentry
{2-SAT}
{\texttt{ts.clause(...)} adiciona cláusulas. \texttt{ts.solve()} decide. \texttt{ts.val[i] = }valor final da variável \texttt{i}. $O(V+E)$.}
{Resolvedor de 2-SAT sobre variáveis 0-indexadas. Interface: crie \texttt{TwoSat ts(n)}, adicione cada cláusula $(x_a = va) \lor (x_b = vb)$ com \texttt{clause(a, va, b, vb)} e finalize com \texttt{solve()}. Se existir solução, o retorno é verdadeiro e a atribuição escolhida fica em \texttt{ts.val[i]}. Complexidade: $O(V + E)$ no grafo de implicação.}
{graph/2-sat.cpp}

\templateentry
{Matching bipartido máximo}
{\texttt{hopKarp(g, l, r)} atualiza os matchings \texttt{l}/\texttt{r} e retorna o tamanho; $O(E\sqrt V)$.}
{Encontra o melhor pareamento de vértices num grafo bipartido, onde cada vértice está em no máximo um par. Passe a adjacência \texttt{g} da partição esquerda para a direita, além de vetores \texttt{l} e \texttt{r} inicializados com \texttt{-1}; a função \texttt{hopKarp(g, l, r)} devolve o tamanho do matching máximo e escreve os pares em \texttt{l[a]} e \texttt{r[b]}. Complexidade: $O(E\sqrt V)$.}
{graph/hopcroft-karp.cpp}
\subsection{Max Flow}
\subsubsection{Dinic}
\varianttext
{\texttt{addEdge(u, v, cap)} e \texttt{flow(s, t)} para fluxo máximo.}
{Dinic para fluxo máximo. Interface:
\begin{itemize}
	\item \texttt{addEdge(from, to, capacity)} adiciona uma aresta direcionada.
	\item \texttt{flow(s, t)} retorna o fluxo máximo de \texttt{s} para \texttt{t}.
	\item \texttt{flow(s, t, max)} faz o mesmo, mas limita o valor total a \texttt{max}.
\end{itemize}
Complexidade: $O(N^2M)$ no pior caso, e também limitada por $O(FM)$ quando o fluxo máximo é $F$.}
\addtemplate{graph/dinic.cpp}

\subsection{Min Cost Flow}
\subsubsection{MCMF}
\varianttext
{\texttt{addEdge(u, v, cap, cost)} e \texttt{flow(s, t)} para min-cost max-flow.}
{Fluxo de custo mínimo com suporte a custos negativos. Interface:
\begin{itemize}
	\item \texttt{addEdge(from, to, cap, cost)} adiciona uma aresta com capacidade e custo por unidade.
	\item \texttt{flow(s, t)} retorna \texttt{\{max\_flow, min\_cost\}}.
\end{itemize}
Complexidade: $O(VE \cdot flow)$ no pior caso.}
\addtemplate{graph/mcf.cpp}

\templateentry
{Matching bipartido com menor custo}
{\texttt{cost = }custo mínimo. \texttt{match[i] = }coluna escolhida para a linha \texttt{i}. ($n <= m$) $O(n^2m)$.}
{Algoritmo Húngaro para assignment mínimo. Interface: passe uma matriz \texttt{a} de custo \texttt{n x m} com \texttt{n <= m}; \texttt{hungarian(a)} retorna \texttt{\{cost, match\}}, onde \texttt{match[i]} é a coluna escolhida para a linha \texttt{i}. Para maximização, negue os custos ou subtraia de uma constante grande. Complexidade: $O(n^2m)$.}
{graph/hungarian.cpp}
