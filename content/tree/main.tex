\templateentry
{LCA}
{\texttt{LCA lca(g, root)} e \texttt{lca.lca(a, b)}; build $O(n \log n)$, query $O(1)$.}
{LCA por Euler tour + sparse table. Construa \texttt{LCA lca(g, root)} a partir da lista de adjacência e consulte pares com \texttt{lca.lca(a, b)}. Preprocessamento $O(n \log n)$, consulta $O(1)$.}
{tree/lca.cpp}

\templateentry
{HLD}
{Heavy-light decomposition. Complexidade: Construção em $O(n)$, caminhos em $O(\log(n))$.}
{Heavy-light decomposition. Complexidade: Construção em $O(n)$, caminhos em $O(\log(n))$.}
{tree/hld.cpp}

\ifextended
\templateentry
{Diâmetro de uma árvore}
{}
{Encontra o diâmetro da árvore (os dois vértices mais distantes na árvore). \texttt{treeDiam(n, g)} retorna a sequência de vértices no diâmetro da árvore. Complexidade: $O(n)$.}
{tree/tree-diameter.cpp}
\fi

\templateentry
{Matching em Árvores}
{\texttt{treeMatching(g, root)} retorna o parceiro de cada vértice em $O(n)$.}
{Encontra o maior matching na árvore. O retorno é um vetor \texttt{match} com o parceiro de cada vértice, ou \texttt{-1} se ele ficar livre. Complexidade: $O(n)$.}
{tree/tree-matching.cpp}

\templateentry
{Árvore virtual}
{Encontra as arestas da árvore virtual do conjunto no formato \texttt{[par, child]}. $O(k \log(n))$.}
{Encontra o menor conjunto de vértices que contém todos os LCAs dos vértices passados. Retorna as arestas dos LCAs para os filhos imediatos no conjunto no formato \texttt{[par, child]}. Complexidade: $O(k \log(n))$.}
{tree/virtual-tree.cpp}
