\ifextended
\templateentry
{Busca binária}
{}
{Executa busca binária no intervalo \texttt{[lo, hi]}. A condição da busca binária é dada por \texttt{cond}. Assumindo que \texttt{cond(lo) = false} e \texttt{cond(hi) = true}, o algoritmo encontra um ponto \texttt{i} tal que \texttt{cond(i) = false} e \texttt{cond(i + 1) = true}, e retorna este ponto por meio de \texttt{lo = x} e \texttt{hi = x + 1}. Complexidade: $O(\log(\texttt{hi} - \texttt{lo}))$}
{dsa/binary-search.cpp}

\templateentry
{Busca ternária}
{}
{Dada uma função convexa ou unimodal, encontra o ponto máximo da função. Complexidade: $O(\log(\texttt{l} - \texttt{r}))$.}
{dsa/ternary-search.cpp}
\fi

\ifextended
\templateentry
{Árvore de Fenwick}
{BIT 0-indexada: \texttt{add(i, v)} e \texttt{sum(l, r)}.}
{Fenwick para soma em prefixo/range. \texttt{Fenwick fw(n)} cria uma árvore de Fenwick de tamanho \texttt{n}, inicialmente apenas com zeros. Use \texttt{fw.add(i, val)} para adicionar o valor \texttt{val} no índice \texttt{i}, \texttt{sum(r)} para obter a soma em $[0, r)$ e \texttt{sum(l, r)} para obter a soma em $[l, r)$. Complexidade: $O(\log n)$ por operação.}
{dsa/fenwick-tree.cpp}
\fi

\ifextended
\templateentry
{Segtree simples}
{}
{Segtree para uma operação \texttt{op}. Altere o tipo \texttt{S} na linha \texttt{using S = int}, a função \texttt{op} e o elemento neutro \texttt{e}, tal que \texttt{op(x, e) = op(e, x) = x}; construa \texttt{Segtree seg(n)}, altere o valor no ponto \texttt{i} com \texttt{set(i, val)} e calcule $\mathrm{op}(a_l, \dots, a_{r - 1})$ em $[l, r)$ com \texttt{query(l, r)}. Complexidade: $O(\log n)$ por operação.}
{dsa/simple-segtree.cpp}
\fi

\templateentry
{Segtree}
{Segtree iterativa genérica: \texttt{set(i, x)} e \texttt{query(l, r)} em $O(\log n)$.}
{Segtree para qualquer monoide. Defina o tipo \texttt{S}, a função \texttt{op} e o neutro \texttt{e}; construa \texttt{Segtree<S, op, e> seg(n)}, faça updates pontuais com \texttt{set(i, val)} e consultas em $[l, r)$ com \texttt{query(l, r)}. Complexidade: $O(\log n)$ por operação.}
{dsa/segtree.cpp}

\templateentry
{Lazy Segtree}
{Lazy segtree genérica: \texttt{set}, \texttt{apply} e \texttt{sum} em $O(\log n)$.}
{Segtree com lazy propagation. Além de \texttt{S}, \texttt{op} e \texttt{e}, defina o tipo de update \texttt{U}, a aplicação \texttt{mapping}, a composição \texttt{compose} e a identidade \texttt{id}; construa \texttt{LazySegtree<...> seg(n)}, use \texttt{set(i, val)}, \texttt{apply(l, r, upd)} e \texttt{sum(l, r)} em $[l, r)$. Complexidade: $O(\log n)$ por operação.}
{dsa/lazy-segtree.cpp}

\templateentry
{Sparse Table}
{Estrutura estática para idempotentes: build $O(n \log n)$ e query $O(1)$.}
{Sparse table para operações idempotentes, como \texttt{min}, \texttt{max} e \texttt{gcd}. Defina \texttt{op}, faça \texttt{SparseTable<S, op> st}, chame \texttt{build(v)} e consulte intervalos $[l, r)$ com \texttt{query(l, r)}. Construção: $O(n \log n)$. Consulta: $O(1)$.}
{dsa/sparse-table.cpp}

\ifextended
\templateentry
{Disjoint Set Union}
{}
{Estrutura para representar $N$ conjuntos. É capaz de fundir dois conjuntos em um, e verificar se dois dos conjuntos originais foram fundidos eventualmente. Exemplo: inicialmente temos os conjuntos \texttt{\{0\}, \{1\}, \{2\}, \{3\}}, podemos juntar os conjuntos 1 e 3, e os conjuntos 0 e 2 para obter \texttt{\{0, 2\}, \{1, 3\}}, depois podemos checar se 1 e 3 pertencem ao mesmo conjunto ou 1 e 2, etc. Construa \texttt{DSU dsu(n)}, use \texttt{root(x)} para obter o representante do conjunto de \texttt{x} e \texttt{merge(x, y)} para unir dois conjuntos; o retorno de \texttt{merge} indica se a união realmente aconteceu, ou se os conjuntos já estavam juntos. Para verificar se dois elementos estão no mesmo  conjunto, basta verificar se \texttt{dsu.root(x) == dsu.root(y)}. Complexidade amortizada: $O(\alpha(n))$ amortizada por operação.}
{dsa/dsu.cpp}
\fi

\subsection{Convolução}
\varianttext
{FFT e NTT para multiplicar polinômios em $O(n \log n)$.}
{Implementações de convolução rápida. Encontra um vetor $c$, onde $c_k = \sum_{i+j=k} a_i b_j$. Use FFT para reais/inteiros com cuidado numérico e NTT quando houver módulo adequado.}
\subsubsection{FFT}
\addtemplate{dsa/fft.cpp}
\subsubsection{NTT}
\addtemplate{dsa/ntt.cpp}
\templateentry
{Convolução de XOR}
{\texttt{xorConv(a, b)} para convolução por XOR em $O(n \log n)$.}
{Dados arrays $a$ e $b$, encontra o array $c$ tal que \[c_k = \sum\limits_{i \oplus j = k} a_i b_j\]As linhas comentadas mostram como adaptar para módulo. Complexidade: $O(n \log n)$.}
{dsa/xor-convolution.cpp}

\ifextended
\templateentry
{Max Add Segtree}
{}
{Estrutura capaz de calcular o maior valor em um intervalo, e adicionar um valor a cada número em um intervalo. Construa \texttt{MaxAddSegtree seg(n)}. Use \texttt{update(l, r, v)} para somar \texttt{v} em $[l, r)$ e \texttt{query(l, r)} para obter o máximo no intervalo. Complexidade: $O(\log n)$ por operação.}
{dsa/max-add-segtree.cpp}
\fi

\templateentry
{Zeta e Mobius}
{Transformadas em subconjuntos para vetores indexados por máscara, $O(B2^B)$.}
{Calcula soma de valores em subconjuntos/superconjuntos/divisores/múltiplos. Complexidade: $O(n\log(n))$.}
{dsa/zeta.cpp}

\templateentry
{Árvore de Li Chao}
{Envelope de retas para mínimo: \texttt{insert} e \texttt{query} em $O(\log N)$.}
{Estrutura que mantém um conjunto de retas, e encontra para cada valor de $x$ a reta com o menor valor de $y$ naquela posição. \texttt{LiChao lc(N)} constrói uma árvore no intervalo $[0, n)$. Insira retas \texttt{\{a, b\}} com \texttt{insert(line)} e encontre a reta mais baixa no ponto \texttt{x} com \texttt{query(x)}; o retorno é a melhor reta, então use \texttt{line(x)} para obter o menor valor. Complexidade: $O(\log N)$ por inserção/consulta.}
{dsa/li-chao.cpp}

\templateentry
{Árvore de Li Chao++}
{Li Chao com retas ativas só em subintervalos, $O(\log^2 N)$.}
{Mesma coisa que o snippet acima, mas permite inserir retas apenas em um intervalo. Use \texttt{insert(line, L, R)} para ativar a reta apenas em $[L, R)$ e \texttt{query(x)} para obter a melhor reta em \texttt{x}. Complexidade: $O(\log^2 N)$ por inserção e $O(\log N)$ por consulta.}
{dsa/li-chao-extended.cpp}

\ifextended
\templateentry
{Maior subsequência crescente.}
{}
{Encontra a maior subsequência crescente do array \texttt{a}. Retorna \texttt{[id, par]}, onde \texttt{id} é a lista dos índices de uma maior subsequência crescente, e \texttt{par[i]} é o índice que deve ser escolhido antes de \texttt{i} para formar a maior subsequência crescente, ou \texttt{-1} se não existe tal índice. Complexidade: $O(n \log(n))$}
{dsa/lis.cpp}
\fi

\templateentry
{Árvore Cartesiana}
{\texttt{cartTree(n, le)} retorna o pai de cada índice em $O(n)$.}
{Constrói a árvore cartesiana dos índices $0, \dots, n-1$, uma árvore binária onde a raiz é o menor valor do array, os filhos são os menores valores de cada lado do array. Passe \texttt{n} e um comparador \texttt{le(i, j)} dizendo quando \texttt{i} deve ficar acima de \texttt{j} na árvore. O retorno é o vetor \texttt{par}, onde \texttt{par[v]} é o pai de \texttt{v}. Complexidade: $O(n)$.}
{dsa/cartesian-tree.cpp}

\templateentry
{Treap implícita}
{Treap de sequência com \texttt{split}/\texttt{merge}; custo esperado $O(\log n)$.}
{Treap implícita para representar uma sequência. Use \texttt{split(t, k, a, b)} e \texttt{merge(a, b)} para quebrar/juntar por posição; há helpers prontos como \texttt{ins(rt, p, v)}, \texttt{era(rt, p)}, \texttt{kth(t, p)} e \texttt{sep(rt, l, r, a, b, c)}. Também suporta reversão lazy e soma em intervalo. Complexidade esperada: $O(\log n)$ por operação.}
{dsa/treap.cpp}

\templateentry
{Simplex}
{Maximiza $c \dot x$ dado $Ax \le b$.}
{Maximiza o valor de
\[
	c_0x_0 + c_1x_1 + \dots c_{n-1}x_{n-1}
\]
dado que
\[
A_{i0}x_0 + A_{i1}x_1 + \dots + A_{i(n-1)}x_{n-1} \le b_i
\]
para cada \texttt{i}.
}
{dsa/simplex.cpp}

\templateentry
{Wavelet Matrix}
{Wavelet matrix do array \texttt{a}. \texttt{rangeKth(wm, H, l, r, k)}. \texttt{cntLess(wm, H, l, r, x)}.}
{Wavelet matrix do array \texttt{a}. Estrutura para fazer operações em intervalos relacionadas à representação binária dos números no array. \texttt{rangeKth(wm, H, l, r, k)} encontra o k-ésimo menor elemento no intervalo \texttt{[l, r)}. \texttt{cntLess(wm, H, l, r, x)} conta quantos elementos no intervalo \texttt{[l, r)} são menores que x. Complexidade de construir a wavelet matrix: $O(HN)$. Complexidade das operações: $O(H)$.}
{dsa/wavelet-matrix.cpp}

\templateentry
{Wavelet Matrix++}
{Wavelet Matrix do array \texttt{a}. Retorna \texttt{[wm, pos]}, onde \texttt{wm} é a wavelet matrix, e \texttt{pos[h][i]} diz a posição do elemento \texttt{i} do array na altura \texttt{h}. \texttt{rectOp(wm, l, r, a, b, f)} chama a função \texttt{f} em cada intervalo da wavelet matrix que representa os elementos no intervalo \texttt{[l, r)} do array que estão em \texttt{[a, b)}.}
{Wavelet Matrix do array \texttt{a}. Retorna \texttt{[wm, pos]}, onde \texttt{wm} é a wavelet matrix, e \texttt{pos[h][i]} diz a posição do elemento \texttt{i} do array na altura \texttt{h}. \texttt{rectOp(wm, l, r, a, b, f)} chama a função \texttt{f} em cada intervalo da wavelet matrix que representa os elementos no intervalo \texttt{[l, r)} do array que estão em \texttt{[a, b)}.}
{dsa/wavelet-matrix-extended.cpp}
